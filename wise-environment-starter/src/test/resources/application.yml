spring:
  config:
    activate:
      on-profile: application1

wise.profiles.active: pci_plastic_staging

---

spring:
  config:
    activate:
      on-profile: application2

wise.profiles.active: development, staging, pci_plastic_staging