spring:
  config:
    activate:
      on-profile: application1

wise.environments.active: pci_plastic_staging

---

spring:
  config:
    activate:
      on-profile: application2

wise.environments.active: development, staging, pci_plastic_staging